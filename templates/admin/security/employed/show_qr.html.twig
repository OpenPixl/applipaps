{% extends 'base.html.twig' %}

{% block title %}Employed{% endblock %}

{% block content %}
<div class="container mx-auto mt-16">
    <div class="flex flex-col w-full px-2 mb-2">
        <h1 class="uppercase font-bold text-xl text-emerald-900 w-full">Partagez l'application</h1>
    </div>
    <div class="flex flex-col w-full px-2 mb-2">
        <img src="{{ asset('prescriptors/'~ employed.firstName ~ '_' ~ employed.lastName ~ '/' ~ employed.qrcodePwa) }}" alt="{{ employed.qrcodePwa }}">
    </div>
</div>
{% endblock %}
