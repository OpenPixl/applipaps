<!-- Navbar -->
<nav class="bg-white fixed top-0 left-0 w-full shadow-md py-2 px-4 flex items-center justify-between z-50">
    <!-- Logo et titre -->
    <div class="flex items-center space-x-2">
        <!-- Logo -->
        <img src="{{ asset('images/png/LogoPAPSimmo.png') }}" alt="Logo PAPSimmo" class="h-8">
        <!-- Titre de l'application -->
        <span class="text-red-600 font-bold text-lg">AppliPAP's</span>
    </div>

    <!-- Espace utilisateur -->
    <div class="flex items-center space-x-3">
        {% if app.user %}
            <!-- Icône utilisateur -->
            <a href="#"
               class="bg-red-600 text-white rounded-full p-2 hover:bg-red-700 focus:outline-none focus:ring focus:ring-red-300">                <!-- Icône Tailwind Heroicons -->
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5.121 17.804A8.962 8.962 0 0112 15a8.962 8.962 0 016.879 2.804M12 12a4 4 0 100-8 4 4 0 000 8z" />
                </svg>
            </a>
        {% else %}
            <!-- Bouton Connexion -->
            <a href="#"
               class="text-sm bg-gray-800 text-white py-1 px-3 rounded-md hover:bg-black focus:outline-none focus:ring focus:ring-gray-500"
            >
                Connexion
            </a>
        {% endif %}
    </div>
</nav>